import SpecimenView from '@/components/dc/SpecimenView';

/** In-app product page: /specimen-detail?id=<id> (also the Vercel fallback for /specimen/<slug>). */
export default function SpecimenDetailPage() {
  return <SpecimenView />;
}
