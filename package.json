{
  "name": "celestial-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build && node scripts/capacitor-export.js",
    "build:static": "next build && node scripts/capacitor-export.js",
    "start": "next start",
    "lint": "eslint",
    "android:dev": "npm run build:static && npx cap sync android && npx cap run android",
    "android:build": "npm run build:static && npx cap sync android && cd android && ./gradlew clean assembleDebug",
    "android:open": "npx cap open android",
    "android:sync": "npm run build:static && npx cap sync android",
    "android:clean": "cd android && ./gradlew clean",
    "android:release": "npm run build:static && npx cap sync android && cd android && ./gradlew bundleRelease",
    "android:test": "npm run build:static && npx cap sync android && npx cap run android",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:run": "npx cap run android",
    "cap:open": "npx cap open android",
    "clean": "rm -rf .next out android/app/build android/build",
    "clean:build": "npm run clean && npm run build:static"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/app": "^7.1.0",
    "@capacitor/cli": "^7.4.3",
    "@capacitor/core": "^7.4.3",
    "@capacitor/device": "^7.0.2",
    "@capacitor/dialog": "^7.0.2",
    "@capacitor/haptics": "^7.0.2",
    "@capacitor/network": "^7.0.2",
    "@capacitor/splash-screen": "^7.0.3",
    "@capacitor/status-bar": "^7.0.3",
    "@capacitor/toast": "^7.0.2",
    "capacitor-plugin-safe-area": "^4.0.0",
    "clsx": "^2.1.1",
    "convex": "^1.27.0",
    "dotenv": "^17.2.2",
    "lucide-react": "^0.544.0",
    "next": "15.5.3",
    "next-auth": "^4.24.11",
    "pushy-cordova": "^1.0.61",
    "pushy-cordova-receiver": "file:node_modules/pushy-cordova/receiver",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "recharts": "^3.2.1",
    "zustand": "^5.0.8"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20.19.15",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.5.3",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
